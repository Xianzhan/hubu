eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 2222

spring:
  application:
    name: hubu-service-corp
  data:
    redis:
      timeout: 10000
  redis:
    redisson:
      file: classpath:redisson.yaml
